\documentclass[scrreprt,colorback,accentcolor=tud9b, 11pt]{tudreport}

%most of packages just copy&paste, not checked whether all are necessary!
\usepackage[utf8]{inputenc}                     % sets the input encryption to UTF8 which is always preferable
\usepackage[T1]{fontenc}                        % standard package for selecting font encodings
%\usepackage[latin1]{inputenc}
\usepackage{subfiles}                           % allows to handle multi-file projects more comfortably
\usepackage[ngerman]{babel}                            % support for new German orthography (neue deutsche Rechtschreibung)							% Package für Deutsche Syntax
\usepackage{framed}                             % supports framed or shaded regions that can break across pages
\usepackage{amsmath}                            % highly recommended for serious mathematical typesetting in LaTeX
\usepackage{graphicx}                           % enhanced support for graphics
    \DeclareGraphicsExtensions{.pdf,.jpg,.png}  % defines the standard extensions of graphics
    \graphicspath{{images/}{../images/}}		% necessary when using the subfile-package to express the path to images
\usepackage{siunitx}                            % properly typeset numbers and units
\usepackage{algorithmic}                        % to include e.g. pseudocodes
\usepackage{listings}                           % to insert code fragments
\usepackage{subcaption}                         % allows usage of subfigures
\usepackage{xcolor}								% allows advanced colors
\usepackage{tikz}                               % to make drawings directly with LaTeX
\usepackage{enumitem}                           % allows more flexible enumerate environments
\usepackage{gensymb}							% for generic symbols like \degree


\usepackage{mathtools}
\usepackage{csvsimple}							% enables relatively simple import of csv-data and especially printing them to tables (for plotting, prefer pgfplotstable)
\usepackage{nameref}							% allows to reference to the name (not number) of a labelled entry


% use temf macros
\let\Laplace\zz % workaround since clash with trfsigns
\usepackage{temf-names}
\let\zz\Laplace
\usepackage{temf-finite}
\usepackage{temf-fields}
\usepackage{temf-matrixvector}
\usepackage{notInTemfMacros}


%additional packages
\usepackage{pgfplots}
\usepackage{pgfplotstable}
\usepgfplotslibrary{groupplots}
\pgfplotsset{compat=newest}
	\pgfplotsset{
        table/search path={../csv, ./csv}, 
    }		% adds the paths to search for data when pgfplots-commands import / use files (compare graphicx-package)
\usepgfplotslibrary{units}
\usetikzlibrary{plotmarks}
\usetikzlibrary{patterns}
\usepackage{multicol}
\usepackage{xfrac}
\usepackage{nicefrac}							% allows fractions showing the / slash and numbers next to it
\usepackage[siunitx,americaninductors]{circuitikz}
\usetikzlibrary{calc}
\usetikzlibrary{shapes.geometric }
\usetikzlibrary{datavisualization}
\usetikzlibrary{decorations.pathreplacing}
\usetikzlibrary{intersections}
\usetikzlibrary{circuits.ee.IEC}
\usetikzlibrary{patterns}
\pgfdeclarelayer{b1}
\pgfdeclarelayer{b2}
\pgfsetlayers{b1,main,b2}   %% some additional layers for demo
\pgfplotsset{compat=newest}

\usepackage{float} %für Positionierung der Bilder

\definecolor{lightred}{rgb}{1,0.7,0.7}
 
% \pgfdeclarelayer{main}    % declare background layer
% \pgfdeclarelayer{bg}    % declare background layer
%  \pgfdeclarelayer{bg2}    % declare background layer
%\pgfsetlayers{main,bg2,bg}  % set the order of the layers (main is the standard layer)


\let\stdcaption\caption
\usepackage{float}
\let\caption\stdcaption


\tikzset{
  edgenumber/.style={color=blue, font=\tiny },
  nodenumber/.style={color=black, above right, font=\tiny}
}

\usepackage{caption}
\raggedbottom

\let\zz\ifnumbered{}                            % BEGIN fixes a problem of compatibility of mcode with \chapter command from koma script
\usepackage[framed, numbered]{mcode}            % to properly format matlab code when using a listing (there is a problem with the \chapter command)
\let\ifnumbered\zz{}                            % END fixes a problem of compatibility of mcode with \chapter command from koma script

\usepackage[pdftex,bookmarks=true]{hyperref}    % enables hyperlinks inside the document (e.g. for the table of contents to jump to chapters directly)


%new commands to make coding shorter and make styling more simple 

% makes it possible to create new commands with subscripts witout throwing errors when using them as normal character
\makeatletter %see internet :) 
\newcommand\newsubcommand[3]{\newcommand#1{#2\sc@sub{#3}}}
\def\sc@sub#1{\def\sc@thesub{#1}\@ifnextchar_{\sc@mergesubs}{_{\sc@thesub}}}
\def\sc@mergesubs_#1{_{\sc@thesub#1}}
\makeatother


\def\promille{\, \ensuremath{{}^\text{o}\mkern-5mu/\mkern-3mu_\text{oo}}}
\renewcommand{\eqref}[1]{Glg.~(\ref{#1})}
\newcommand{\mock}{\lstinline{mock}}
\newcommand{\tabref}[1]{Tab.~(\ref{#1})}
\newcommand{\figref}[1]{Abb.~(\ref{#1})}
\newcommand{\ofomega}{ \left( \omega \right) }
\newcommand{\oft}{ \left( t \right) }
\newcommand{\Hcompl}{\underline{H}}			%because \H is already a defined command
\newsubcommand{\Uout}{U}{out}				% see above definition
\newsubcommand{\Uoutc}{\underline{U}}{out}
\newsubcommand{\Uin}{U}{in}
\newsubcommand{\Uinc}{\underline{U}}{in}
\newsubcommand{\Uquest}{U}{?}
\newsubcommand{\Uquestc}{\underline{U}}{?}
\newsubcommand{\fabs}{ \; \mathit{f}}{\mathrm{abs}}
\renewcommand{\mock}{\lstinline{mock}}



\makeatletter
%\@ifundefined{subRoot}{\newcommand{\subRoot}[1]{../#1}}
\@ifundefined{importTikz}{\newcommand{\importTikz}[1]{\input{../images/plots/#1}}} % hard coding to input tex-files (tikz) in subfiles to enable separate compiling (just useful with starting command in begin document

\begin{document}

	\renewcommand{\importTikz}[1]{\input{./images/plots/#1}}			%will not be read by subfiles
		% hard-coding to input tex-files (tikz) in subfiles to enable separate compiling

    \title{-------------------------------------}
    \subtitle{Artem Moskalew \\ Jonas Christ \\ Maximilian Nolte}
    \maketitle

    \tableofcontents
    
    
	%\subfile{./chapter/test}
	
	\subfile{./chapter/Abstract}
	\subfile{./chapter/Einfuehrung}
	\subfile{./chapter/Vorgehen}
	\subfile{./chapter/Code-Design}
	\subfile{./chapter/Optimierung}
	\subfile{./chapter/Fazit}
	\subfile{./chapter/Ausblick}
	\subfile{./chapter/Anhang}



%\listoffigures

\end{document}